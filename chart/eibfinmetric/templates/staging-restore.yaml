{{- if .Values.db.staging.restore.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "restore-staging-db-job.fullname" . }}
  namespace: {{ include "namespace" . }}
  labels:
    {{- include "risk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded

spec:
  template:
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ include "staging.backup.image" . }}
        imagePullPolicy: {{ .Values.db.staging.restore.image.pullPolicy | default .Values.risk.default.image.pullPolicy }}
        env:
          - name: "url"
            value: {{ include "mongo.connection.staging.restore" . }}
          - name: nsFrom
            value: {{ .Values.db.staging.name }}
          - name: nsTo
            value: {{ .Values.db.staging.name }}
          - name: db
            value: staging_db
      restartPolicy: Never
  backoffLimit: 1
{{- end }}